import { getAllWeights } from "./src/bgg/scraper";

/*
declare var require: any
const axios = require('axios');
const jsdom = require("jsdom");
const { JSDOM } = jsdom;
const fs = require('node:fs');
*/

// all logic is stored here for now
getAllWeights();